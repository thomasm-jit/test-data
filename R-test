# loads ggplot2

library("ggplot2")

#assigns mtcars to data
data <- mtcars

#plots graph
ggplot(data, aes(x = hp, y = mpg)) + geom_point() + labs(title = "MPG vs. Horsepower", x = "Horsepower", y = "MPG")

